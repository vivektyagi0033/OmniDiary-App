name: Update Events
on:
  push:
    branches: [ "main" ]
  schedule:
    - cron: '0 0 * * *'  # Daily at midnight UTC
  workflow_dispatch:      # Manual trigger option

permissions:
  contents: write        # Explicitly grant write permission

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Get complete history

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install requests pillow  # ← Added 'pillow' for image processing

      # NEW STEP ADDED HERE ▼ (between pip install and scrape_holidays.py)
      - name: Download Holiday Sprites
        run: |
          python scripts/download_sprites.py

      - run: python scripts/scrape_holidays.py

      - name: Commit and Push
        if: success()
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add events/holidays.json sprites/  # ← Added sprites/ to tracked files
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Auto-update holiday data and sprites [skip ci]"
            git push
          fi
